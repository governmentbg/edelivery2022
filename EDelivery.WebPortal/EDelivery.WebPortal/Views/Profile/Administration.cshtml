@{
    ViewBag.Title = EDeliveryResources.Common.TitleProfileAdministration;
}

<div class="cols-wrapper">
    @Html.Partial("_LeftMenu")

    <div class="content-right">
        <h2>@Common.TitleProfileAdministration</h2>

        <a href="@Url.Action("History","Profile")"
           class="btn btn-border btn-m">
            @Common.TitleProfileAccessHistory
        </a>

        @Html.Action("ProfileInfo")

        @Html.Action("ProfileAttachedDocuments")

        @Html.Action("ProfileAccess")
    </div>
</div>
